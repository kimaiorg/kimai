model Example {
    id   Int     @id @default(autoincrement())
    name String?

    @@map("examples")
}

model User {
    id   Int     @id @default(autoincrement())
    name String?

    @@map("users")
}
